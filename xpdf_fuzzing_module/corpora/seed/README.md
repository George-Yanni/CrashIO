# Seed PDFs

Place a few small PDF files here before fuzzing (AFL++ uses them as starting inputs).

From the module root you can run:

```bash
./scripts/fetch_seeds.sh
```

Or copy any legitimate PDFs you already have into this directory.
