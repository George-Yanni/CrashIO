# Seed corpus (introductory module)

This folder is **generated** (binary seeds are created by `scripts/gen_seeds.py`).

Why generate seeds instead of committing binaries?
- Keeps the repository clean and diff-friendly.
- Lets you tweak the format and regenerate deterministically.

The automation scripts will create a tiny initial corpus here, then copy it into a working corpus directory and grow/minimize it.

